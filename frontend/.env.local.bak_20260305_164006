# DECIDE frontend local overrides (highest priority in Next dev)
# Force backend to FastAPI on :8000 (NOT 8090)
DECIDE_BACKEND_URL=http://127.0.0.1:8000
BACKEND_URL=http://127.0.0.1:8000

# If any client-side code uses a public var:
NEXT_PUBLIC_DECIDE_BACKEND_URL=http://127.0.0.1:8000