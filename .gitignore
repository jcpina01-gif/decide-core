node_modules/
**/node_modules/

# Python (inclui .venv311, .venv, etc.)
.venv*/
**/.venv*/
venv/
ENV/

# Dados locais / backups enormes
.data/
_ARCHIVE_BACKUPS/

__pycache__/
*.py[cod]
.pytest_cache/

# Snapshots / cópias locais (muito grandes)
backend/RELEASES/
backend_CORRUPTED*/

# Segredos e ambiente local
.env
.env.local
**/.env.local

# Next (build)
.next/
next-cache/
**/next-cache/

# Raiz do clone: só README + frontend/ são do projeto; o resto são cópias locais / notas / logs
/*
!/.gitignore
!/README.md
!/frontend/
# API FastAPI (Render): só estes ficheiros (há pasta backend/ local com muito lixo ignorado)
!/backend/
/backend/*
!/backend/server.py
!/backend/requirements.txt
# KPI Flask (iframe do client-dashboard) — ficheiro na raiz; dados em freeze/ (deploy separado ou disco)
!/kpi_server.py
!/requirements-kpi.txt
