# Copiar para `.env.kpi` na raiz do clone (mesmo nível que `kpi_server.py`).
# Esse ficheiro é carregado por `restart_decide_kpi_gunicorn.sh` antes de arrancar o Gunicorn.
# Não fazer commit de `.env.kpi` com segredos.
#
# Curvas = NAV do freeze sem *rail* de sanidade (escala log pode ficar ilegível se o CSV tiver cauda corrupta):
# DECIDE_KPI_EQUITY_RAIL=0
